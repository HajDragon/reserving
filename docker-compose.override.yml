services:
  app:
    environment:
      - APP_DEBUG=1
      - XDEBUG_MODE=off
    ports:
      - "9000:9000"
    networks:
      - default

  node:
    command: ["npm", "run", "dev"]
    environment:
      - CHOKIDAR_USEPOLLING=true

networks:
  default:
    driver: bridge
